import { Skeleton } from "@/components/ui/skeleton";

const ThumbnailSkeleton = () => {
  return <Skeleton className="absolute left-0 top-0 h-full w-full rounded" />;
};

export default ThumbnailSkeleton;
